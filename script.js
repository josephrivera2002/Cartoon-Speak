$("#talk").click(function() {
    console.log($("#speech").val());
    // 1. Save the input value to a variable
    var message=$("input").val();
    // 2. Set the text of #speech-bubble to the saved value
    
    
});